def normalizepicture(filepath):
    #this will take in a filepath and generate a new image that fits the size required by the ML model, it will return a file path
    outputFilepath = ''
    return outputFilepath
